{{#section 'head'}}
    <title>商品详情</title>
    <link rel="stylesheet" href="/css/goods_details.css">
{{/section}}

{{>topbar}}

<!--header start-->
{{>header_search}}
<!--header end-->

<!--nav start-->
{{>nav}}
<!--nav end-->
{{#with results}}
<input type="hidden" name="goodsId" value="{{id}}">
<!--商品详情 start-->
<div class="content_w">
    <div class="clearfix">
    <div class="imgPic f-l clearfix">
        {{#with thumbs}}
            <div class="pic f-l"><img src="{{#getOne 0}}{{/getOne}}"></div>
        {{/with}}
        <div class="list f-l">
            <a href="javascript:;" class="arrow arrow_top"></a>
            <div class="list_wrapper">
                <ul style="transform: translateY(0px);">
                    {{#each thumbs}}
                        <li><a href="javascript:;"><img src={{this}}></a></li>
                    {{/each}}
                </ul>
            </div>
            <a href="javascript:;" class="arrow arrow_bot"></a>
        </div>
    </div>
    <div class="desc f-l">
        <h2>{{#if goodsname}}{{goodsname}}{{else}} / {{/if}}</h2>
        <p>原产国/地：{{#if country}}{{country}}{{else}} / {{/if}}</p>
        <p>货产国/地：{{source}}</p>
        <p>型号：{{model}}</p>
        <p>毛重：{{#if GW}}{{GW}}{{else}} / {{/if}}kg</p>
        <p>商品规格：{{#if LWH}}{{LWH}}{{else}} / {{/if}}</p>
        <p>储存方式/注意事项：{{#if needAttention}}{{needAttention}}{{else}} / {{/if}}</p>
        <a href="javascript:;" class="pur_btn">购买/采购</a>
    </div>
    <!--<a href="javascript:;" class="backToList">返回我的商品列表</a>-->
    </div>
    <div class="goods_details_pics">
        {{#each contents}}
            <img src="{{this}}"/>
        {{/each}}
    </div>
</div>
{{/with}}
<!--商品详情 end-->

{{#section 'jquery'}}
    <script src="/js/public.js"></script>
    <script type="text/javascript">
        ele.imgPicList.on('click','li a', function () {
            var src = $(this).find('img').attr('src');
            ele.imgPic.find('img').attr('src',src);
        });

        var num = 0;
        $('.arrow').click(function () {
            $ul = $('.list_wrapper ul');
            var liLen = ele.imgPicList.find('li').length;
            var tans = $ul.css('transform').split(',');
            var tanslateY = parseInt(tans[tans.length-1]);
            console.log(tanslateY);

            /*if($(this).hasClass('arrow_top')){
                if( liLen - (tanslateY/110)  >= 4){
                    return false
                }else {
                    num+=110;
                    ele.imgPicList.find('ul').css('transform','translateY('+num+'px)');
                }
            }else {
                if( liLen + (tanslateY/110)  <= 4){
                    return false
                }else {
                    num-=110;
                    ele.imgPicList.find('ul').css('transform','translateY('+num+'px)');
                }
            }*/

        })
    </script>
{{/section}}